# =====================================
# Spring Boot Application Configuration
# =====================================
spring.application.name=k23cnt3-nhd-lesson06
server.port=8080

# =====================================
# DataSource Configuration (MySQL)
# =====================================
spring.datasource.url=jdbc:mysql://localhost:3306/lesson06db?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=huuduy
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# =====================================
# JPA / Hibernate Configuration
# =====================================
spring.jpa.show-sql=true
spring.jpa.hibernate.ddl-auto=update
# Hibernate s? t? ??ng ch?n dialect cho MySQL, nên không c?n khai báo n?a
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# =====================================
# Optional: Disable Open-In-View to avoid lazy-loading warnings
# =====================================
spring.jpa.open-in-view=false
